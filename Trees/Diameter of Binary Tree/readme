Naive Approach :

TC : O(n2) because extra O(n) took for calculating height of tree.
SC : O(height)

Optimise Approach: using pairs
TC: O(N)
SC: O(H)

Optimise Approach: calculating height and diameter at a same time
TC: O(N)
SC: O(H)
